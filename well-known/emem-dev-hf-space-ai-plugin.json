{
  "schema_version": "v1",
  "version": "1.1.0",
  "name_for_human": "emem shared memory",
  "name_for_model": "emem",
  "description_for_human": "Shared, verifiable memory for AI agents, robots, and sensing platforms: a citeable identity layer that stops referential drift. Every place, fact, and object gets one canonical citeable identity (cell64, fact_cid, and emem:entity: via emem_entity), so different models reason from the same world object instead of divergent descriptions. Earth-scale signed facts + writable agent memory, both ed25519-signed and receipt-verifiable.",
  "description_for_model": "Shared, verifiable memory for AI agents: a vendor-neutral, citeable identity layer that stops referential drift, so different models reason from the same world object instead of divergent descriptions. Every place resolves to one canonical address (cell64), every observation to one signed fact (fact_cid), and every object to one citeable identity (emem:entity:<entity_cid>) minted by emem_entity, the object-level antidote to drift. Grounded in signed Earth observation. Two layers, one trust surface. Earth-scale spatial fact store: every patch of ground gets a 64-bit address (cell64, about 9.55 m), and every measurement at that cell is a signed fact keyed by (cell, band, tslot), ed25519-signed by the responder over the blake3 of its canonical CBOR. Agent memory: above the fact store sits the writable scratchpad the agent owns: six Anthropic memory-tool verbs (memory_view/create/str_replace/insert/delete/rename), CoALA-typed files (episodic/semantic/procedural/resource), capability-bound writes under /memories/by_attester/, BGE-768 semantic search over notes, multi-attester contradiction scoring per band kind, signed bundles (emem:bundle:<bundle_cid>) and object identities (emem:entity:<entity_cid>), and bi-temporal recall (as_of_tslot for valid time, as_of_signed_at for transaction time, both threaded through every read). 94 MCP tools (14 core, 80 extended); tools/list at /mcp advertises the core tier, /mcp/full advertises all 94, and every tool is callable by name from either, with emem_tools returning the map or a single tool's schema on demand; resources, algorithms, bands, and sources are enumerated live by tools/list, /v1/bands, and /v1/sources. 4 foundation encoders (Clay v1.5, Prithvi-EO-2.0-300M-TL, Tessera, Galileo), three GPU-pinned and Tessera streamed from Cloud-Optimized GeoTIFF on CPU, behind a 1792-D state cube. Every response carries an ed25519 receipt verifiable offline at /verify. No API keys.",
  "auth": {
    "type": "none"
  },
  "api": {
    "type": "openapi",
    "url": "/openapi.json"
  },
  "logo_url": "/logo.png",
  "contact_email": "avijeet@vortx.ai",
  "legal_info_url": "/terms"
}
