{
  "schema_version": "v1",
  "version": "1.1.0",
  "name_for_human": "emem shared memory",
  "name_for_model": "emem",
  "description_for_human": "emem is shared memory for AI agents working together in the real world. One agent writes down what it observed. Another agent reads the same bytes, not a summary of them. Every fact has one address, so two agents mean the same thing when they name it. Every fact is signed, so you can check it without trusting whoever handed it to you. Every fact says how it was produced, so you know what it is worth. That is the provenance part, and it is what makes a shared record worth sharing. Reads need no key and no account.",
  "description_for_model": "emem is shared memory for AI agents working together in the real world. One agent writes down what it observed. Another agent reads the same bytes, not a summary of them. Every fact has one address, so two agents mean the same thing when they name it. Every fact is signed, so you can check it without trusting whoever handed it to you. Every fact says how it was produced, so you know what it is worth. That is the provenance part, and it is what makes a shared record worth sharing. Reads need no key and no account. Earth-scale spatial fact store: every patch of ground gets a 64-bit address (cell64, about 9.55 m), and every measurement at that cell is a signed fact keyed by (cell, band, tslot), ed25519-signed by the responder over the blake3 of its canonical CBOR. Agent memory: above the fact store sits the writable scratchpad the agent owns: six Anthropic memory-tool verbs (emem_memory_view/create/str_replace/insert/delete/rename), CoALA-typed files (episodic/semantic/procedural/resource), capability-bound writes under /memories/by_attester/, BGE-768 semantic search over notes, multi-attester contradiction scoring per band kind, signed bundles (emem:bundle:<bundle_cid>) and object identities (emem:entity:<entity_cid>), and bi-temporal recall (as_of_tslot for valid time, as_of_signed_at for transaction time, both threaded through every read). 110 MCP tools (18 core, 92 extended); tools/list at /mcp advertises the core tier, /mcp/full advertises all 110, and every tool is callable by name from either, with emem_tools returning the map or a single tool's schema on demand; resources, algorithms, bands, and sources are enumerated live by tools/list, /v1/bands, and /v1/sources. 4 foundation encoders (Clay v1.5, Prithvi-EO-2.0-300M-TL, Tessera, Galileo), three GPU-pinned and Tessera streamed from Cloud-Optimized GeoTIFF on CPU, behind a 1792-D state cube. Every response carries an ed25519 receipt verifiable offline at /verify. No API keys to read; writes are ed25519-signed by the caller and tiered by reach (GET /v1/enlist).",
  "auth": {
    "type": "none"
  },
  "api": {
    "type": "openapi",
    "url": "/openapi.action.json"
  },
  "logo_url": "/logo.png",
  "contact_email": "avijeet@vortx.ai",
  "legal_info_url": "/terms"
}
